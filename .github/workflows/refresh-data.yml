name: Refresh market data

on:
  schedule:
    # 美股收盘后：21:30 UTC ≈ 美东 17:30（夏令时）/ 16:30（冬令时），周一至周五
    - cron: "30 21 * * 1-5"
    # 开盘前补一刀：13:00 UTC ≈ 美东 9:00，接住 Yahoo 迟到的日线
    - cron: "0 13 * * 1-5"
  workflow_dispatch: {}   # 手动触发按钮（Actions 页面随时点）

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies (core pipeline only)
        run: pip install yfinance pandas numpy scipy requests

      - name: Run pipeline
        run: python market-analysis/scripts/run_all.py

      - name: Commit & push if data changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "chore: auto-refresh market data [skip ci]"
          git push
