# Valpha Lab — Claude Code 工作约定

## 用户与访客（别当过时信息清理）

- 用户在**澳洲阿德莱德**（UTC+9:30）：AUD 持仓计算器、澳洲 CGT 计算器、"本地日期比美东快一天"的注释都是真实需求
- 网站有**中国境内访客**：前端资源同源自托管（Plotly 已本地化），不要引入境外第三方 CDN 依赖

## 当前状态（2026-06-19 更新）

- ✅ P0/P1/P2/P4 全完成；06-14 全库 Opus 三方审查(统计/安全/前端)主项全修——**S1/S6/S5/S2 已全部完成**(详见 `LOOP_DECISIONS.md`)；06-15 验证强化批(#1 CPCV/PBO·#2 VIX体制保形·#3 校准漂移·#4 路径回撤)均独立审过上线
- ✅ **Valpha Lab 三层体系 + 工具页全建好（06-19）**：🛰️Valpha150大盘(152票·日刷) · 📊板块 · 📡雷达(综合评分) · 🎲配置器(AUD·CGT币种切换) · 🔥野蛮 · 🛫IPO三档 · 🛡️方法论 · 🌱v2.0自生长(跨族FDR知识库+衰减追踪)；index.html=根入口、dashboard.html=完整仪表盘
- ✅ **5 路多维审查 + 修复闭环（06-19）**：correctness/security/a11y/honesty/simplicity——**诚实审零越界通过**、简洁审确认自包含合理；已修 localStorage防抛(打不开根因)·对比度达AA·esc(ffb43a3) + 9页移动端@media·表头键盘排序·首屏占位(1a0bd09)
- ✅ **#5 分段透镜→factor_audit（74c5f46）+ ② 主盘轻量双语外壳（56eb8df）本会话完成**：#5 揭 2 因子(纳指动量/高波动)现代段被套利、口径异于 OOS、含段位门+三态单测；② 译导航+9 intro+onboarding，深度面板留中文(EN 顶部说明)、自包含内联脚本低风险
- **下一步（均非缺陷，待你拍板）**：① **v1.5 自生长自动发现闭环**(现仅展示层；最大工程，建议单独立项 + 重审) ② 可选更深:conformal 合成覆盖测试·event_study 置换·CSP meta(需浏览器实测) ③ 主盘"全量"双语(现为轻量外壳) · IPO 数据定期更新 · Valpha150 成分自动发现

## 运行环境（Windows）

- 用 `py`（Python 3.12），**不要用 `python`**（指向 Microsoft Store 占位程序）
- 跑任何脚本前设 `$env:PYTHONUTF8='1'`（GBK 控制台会让 Unicode 输出崩溃）
- 全量流水线：`py market-analysis/scripts/run_all.py`（约3分钟）；盘中轻量：加 `--light`
- 测试：`py -m pytest market-analysis/tests -q`（必须全绿才能提交流水线改动）
- 前端改动后跑 `py tools/audit_frontend.py` 交叉检查 ID/onclick

## 模型分层委派（用户授权，自主判断不必先问）

分层委派有效，但**编排与判断固定由 Fable 5（主脑）担任**——别被 `switchModelsOnFlag` 的安全误报
切到 Opus 4.8 当默认（聊土壤/生物/光谱等话题易误判）；被切走就 /model 切回 Fable 5。

委派要"强编排"（2026-06-12 用户要求，P2-3 教训）：主脑的价值在**更详细的规格 + 更重的审查**，不是亲力亲为：
- **写规格**：交代给子代理时预判判断点，写成"若遇到 X（如校准曲线倒挂/非单调），停下报告，不要自行决定"
- **审查**：子代理自报完成 ≠ 验收；实质改动跑 Opus 审查（agent-skills:code-reviewer）+ 主脑亲验关键输出
- **分工**：Fable 5 主脑=方法论/统计判断/含判断的改动/写规格/终审；Sonnet=规格明确无判断的机械活；
  Haiku=重命名/复制级琐事；Opus=审查

### 委派工作流（2026-06-12 用户确认，验证有效）

两种排法按任务类型分流：
- **判断密集型**（统计建模、含诚实性判断）：**Fable 建 + Opus 审**（Opus 冷启动新视角，每轮都抓出主脑盲区）
- **规格明确、实现量大**（聚合/展示/工程，如 benchmark 记分卡）：**Fable 详细计划 → Opus 建 → Fable 审**（卸主脑的载）

六步协议（关键是新增"审计划"）：
```
① 主脑写计划 → ② 独立审计划(挑 spec 洞，比建完再改便宜得多) → ③ 建造
→ ④ 独立审实现 → ⑤ 修复 → ⑥ 验证提交
```
- **审实现按风险分级**：公开统计结论/模型链=双审(Fable+全新Opus)；常规聚合/展示=单审即可，别无脑双审
- **铁律：审查者 ≠ 建造者**（不同模型/全新上下文，保证独立）
- 实证：benchmark 那次 Opus 代码很干净，但 Fable 审出了 Opus 自测漏掉的 ordering bug；而那个 bug 本可在"审计划"阶段更早抓到——所以两道审都有价值

### 与用户的协作风格（用户明确偏好）
用户提建议，**主脑要"考虑 + 平衡最优解"，不是顺着点头**。该踩刹车就踩（如建议无脑双审时分级、建议停委派时厘清是误会）。
诚实的技术异议比虚假认同有价值——这是用户要的。

## 执行协议

- **主脑不是 Fable 5 的会话 → 先读根目录 `HANDOVER.md`**（角色分配/任务队列/CI 红手册/架构路线，2026-07-07 起）
- 按 ROADMAP.md 任务编号干活，**每个编号 = 一次提交**，commit message 带任务号
- **先查后动（用户铁律 2026-06-18）**：收到"做 X"先读 `DAILY_LOG.md` + `OPTIMIZATION_LOG.md §4c(试过但否决)` + 🪦坟场 + 📋登记簿 → 报"之前做过没 / 做成啥样 / 有无结果" → 再动手。**每个尝试都登结果（成/败/测量结论），别只记计划**——失败的尝试也是结果（进坟场或 §4c），这样以后"做 X"一读就清楚
- 改流水线 → 提交前全量跑通 + pytest 全绿；改模型 → bump `MODEL_VERSION` 并重跑 walk_forward，
  新旧指标对比写进 commit message
- web/ 改动要镜像到 docs/（run_all 末步会自动做；手改记得 Copy-Item）
- 提交后 push 到 origin main（GitHub Pages 自动部署；CI 每小时也会自动提交数据，push 前先 fetch）

## 方向与边界（2026-06-22 用户重定调：红线是早期护栏，现逐步突破升级）

**早期"测风险不测方向 / 不预测 / 不荐股 / 不深度学习 / 不引框架"是起步纪律，不是永久红线。**
项目正一点点纳入**预测、荐股、深度学习、LLM 生成**——默认诚实统计打底，前沿逐步扩张。
要守的不是"不预测"，而是**敢预测就敢认账**：每个方向判断都留可追责的公开战绩
（tipjar / 观点 / composite 已是范式）。差异化命门 = **诚实计分**，不是又一个吹票机。
→ 别再拿"不预测/不荐股/不深度学习"拦用户的升级请求（这是我反复犯的错）。

逐步纳入（带验证，不是无脑上；遇判断点照"强编排"停下报告）：
- **预测/荐股**：继续扩，渐成主线；每条带免责 + append-only 计分
- **深度学习**：可试；但小样本易过拟合 → 上线前先 walk-forward/CPCV 证它真比简单模型强（诚实工程异议≠拒绝）
- **LLM 生成**：可做（如"大白话日读"）；**必须喂真算出的数据、防瞎编**；API 密钥/成本/澳洲可达先定
- **新数据源**（社交情绪/另类数据，含此前列的 TickFlow/AkShare 等）：可纳入；先确认够长历史能回测 + 澳洲可达
- **多市场**（A股/港/日韩）：暂不做，后续再说
- **多渠道推送**：Telegram 已接（notify_telegram.py）

仍守（质量地基，非红线）：
- 不绕过 verify_output / pytest 门禁
- prediction_log.csv / paper_ledger.csv / autodiscovery_log.csv 是 append-only 账本，绝不手改历史行
- 秘钥（API token 等）只走环境变量/Secrets，绝不进公开仓库
- 公开计分 / 标不确定性（预测越多越要守——这是 Valpha Lab 的护城河）
