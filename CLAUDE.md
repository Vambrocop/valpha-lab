# Valpha Lab — Claude Code 工作约定

## 用户与访客（别当过时信息清理）

- 用户在**澳洲阿德莱德**（UTC+9:30）：AUD 持仓计算器、澳洲 CGT 计算器、"本地日期比美东快一天"的注释都是真实需求
- 网站有**中国境内访客**：前端资源同源自托管（Plotly 已本地化），不要引入境外第三方 CDN 依赖

## 当前状态（2026-06-19 更新）

- ✅ P0/P1/P2/P4 全完成；06-14 全库 Opus 三方审查(统计/安全/前端)主项全修——**S1/S6/S5/S2 已全部完成**(详见 `LOOP_DECISIONS.md`)；06-15 验证强化批(#1 CPCV/PBO·#2 VIX体制保形·#3 校准漂移·#4 路径回撤)均独立审过上线
- ✅ **Valpha Lab 三层体系 + 工具页全建好（06-19）**：🛰️Valpha150大盘(152票·日刷) · 📊板块 · 📡雷达(综合评分) · 🎲配置器(AUD·CGT币种切换) · 🔥野蛮 · 🛫IPO三档 · 🛡️方法论 · 🌱v2.0自生长(跨族FDR知识库+衰减追踪)；index.html=根入口、dashboard.html=完整仪表盘
- ✅ **5 路多维审查 + 修复闭环（06-19）**：correctness/security/a11y/honesty/simplicity——**诚实审零越界通过**、简洁审确认自包含合理；已修 localStorage防抛(打不开根因)·对比度达AA·esc(ffb43a3) + 9页移动端@media·表头键盘排序·首屏占位(1a0bd09)
- ✅ **#5 分段透镜→factor_audit（74c5f46）+ ② 主盘轻量双语外壳（56eb8df）本会话完成**：#5 揭 2 因子(纳指动量/高波动)现代段被套利、口径异于 OOS、含段位门+三态单测；② 译导航+9 intro+onboarding，深度面板留中文(EN 顶部说明)、自包含内联脚本低风险
### 📌 下一步清单（2026-08-25 全面核实过；**提议前先按这里，别照旧记忆念**）

> **教训（用户 08-25 指出）**：此前这份清单停在 06-19，我照着念菜单，用户点了才发现「早做完了」——
> 核实成本让用户的额度买单。**规矩：给"下一步"之前先核实当前状态；核实完顺手更新本节。**
> 呼应 memory「Show Existing, Act, Don't Redo」。

**已完成（核实日期 08-25，别再当待办）：**
- ✅ **conformal 覆盖验证**：合成覆盖测试(正态/厚尾t/偏态/单调)早在 `test_conformal_coverage.py`；线上实测覆盖 5日 .800/.892 · 20日 .812/.911 · 60日 .815/.935(名义 .80/.90)，且明确不主张时间序切下的理论保证。
- ✅ **event_study 置换检验**（b2be0bd）：当场撤掉"地缘冲击"被 t 检验高估的显著性。
- ✅ **backtest 重叠窗口修正**（f192a6b）：块自助撤掉 4 条假显著(Tier≥4 策略 p .0033→.209 等)。
- ✅ **主盘双语**：`app-1..5.js` **已有 1174 处 `vpL(zh,en)`**，渲染层基本完成。~~"轻量外壳"是过时描述~~。
- ✅ **IPO/insider 数据**：已补到当前(8b02759/21c41a7)；注意这是 **known-limited**，SEC 封 CI 机房 IP，**每 2-3 周需本地手动补**，不是 bug。
- ✅ **CI 静默失败治理**：修 ndx 缺 lxml(1973392) + 守门测试；watchdog 6→11 项(4f84a55)覆盖全部 `|| warning` 产物。

**08-26 当天新增完成：**
- ✅ **统计诚实性大修**：event_study 置换检验(撤"地缘冲击") · backtest 块自助(撤 4 条，含最面向用户的 Tier≥4 策略 p .0033→.209) · walk_forward 分档块自助(再撤 4 条)。共**撤掉 9 条被参数检验高估的"显著"**。
- ✅ **CI 治理**：ndx 缺 lxml 根因修复 + 依赖守门测试；watchdog 6→11 项；全站 40 页 CSP meta(注：留 unsafe-inline 挡不住 XSS、meta 形式拿不到 frame-ancestors，已在 commit 里如实记)。
- ✅ **数据层双语**：registry / lab(benchmark) / 澳股 / research 四大块 **EN 中文归零**；全站 9 视图 270→190 行。共享取值器 `vpD()` 已抽出，后续接入直接用。
- ✅ **自选组合前向跟踪**(c60c22c)：`data/my_portfolio.txt` 填代码即跟踪；入场不可回填 + 防抢跑 + 防前视护栏，默认 `public=false` 不进公开计分。**缺展示页面**。

**08-31 / 09-01 又完成（核实过，别再当待办）：**
- ✅ **块自助口径修正**(a2872a3)：用户质疑"是不是矫枉过正"→ 实测证实我上一版过头了。块必须取在
  **日历序列**上(mask 随之重采样)，此前取在"某档观测子集"的散落数组上 → 系统性保守 1.8-2.8 倍。
  修正后恢复了 SP500 Tier≥4(p .209→.017) 等本该显著的结论。**教训：撤销结论也要跟建立结论一样谨慎。**
- ✅ **澳股六维雷达**(02d4c14)：106 只。零克隆 import 美股 `_stats/_radar`，独立性基准换 ^AXJO。
  刻意**没有**做"综合评分→排名→推荐买哪只"——那个才需要规格+双审，不顺手夹带。
- ✅ **自选组合展示页** `myportfolio.html`：四个模拟盘(各 A$10,000)已上线。
- ✅ **fail-soft 产物监控**(a786f62)：CHECKS 11→19，补澳股整片 + 自选组合 + quotes + event_causal。
  并把规则自动化：`sys.exit(0)` 的脚本若 write_json 了产物，该产物必须在 CHECKS 里，否则测试红。
- ✅ **数据层双语第二批**(c2d8ef2)：placebo/CPCV/持有期/信号状态/FDR caveat 全部出英文 + 两道覆盖率守门。

**09-03 ~ 09-06 完成（核实过，别再当待办）：**
- ✅ **CI 韧性 backlog #1~#4**(c97b35d)：缓存只留可再生数据(31 个被跟踪账本不再进缓存) ·
  re-assert 改整目录复位(无清单可漂移) · `git add -A` 全清零 · 发布逻辑进 `tools/ci_publish.py`
  (真 git 裸仓测四条路径) · 防缩水门 warn-only 重挂。
- ✅ **AI 战绩牌基线修正**(42b4c1c)：原写"三桶随机基线 1/3"是错的(三桶不等概)。
  正确门槛=多数类基线 → 59.0% 其实**不如**"每天闭眼说中性"的 61.5%(p=0.692)。
  颜色也改成低于基线不显示绿色。scorecard 上 n=1 发布"0%"加粗当头条一并修。
- ✅ **观察台裁决横跳标注**(53bdb20)：`unstable` 此前只看 recent_p,不看跨族 FDR 边界横跳。
  线上实测 7 条存活者**5 条在反复改判**(翻转 2~19 次),只有 2 条真稳(0 翻转·稳 44/53 天)。
  顺带修 composite.html 三处只走 github.io 绝对 URL(境内访客绕道 + 本地看不到自己的改动)。
- ⚠️ **我的守门测试把刷新干红两天**(fe8af0a)：CI 用 `--depth=1` 浅克隆,单提交无父提交 →
  `git show --name-only` 列出**整个仓库 556 文件**。且**只在 HEAD 恰好是 bot 提交时**才触发,
  我手动 dispatch 时 HEAD 都是自己的提交 → 跳过 → **假绿**。
  **教训:"我手动跑了一次 CI 是绿的"不等于验证过,还得问 HEAD 是什么形状。**

**09-06 / 09-07 又完成（核实过，别再当待办）：**
- ✅ **提交门禁 CI 同构**(c9ec2a7)·**Valpha150 成分账本 + 池子维护年龄**(ffacdea)
- ✅ **CSP 真浏览器实测**(5cc643e)：42 页零自伤;工具会主动报出边界(带 unsafe-inline → 挡不住 XSS)
- ✅ **LLM 日读/周报/月读英文版**(32b4fb9)：**翻译中文原文,不重新生成**(独立生成会让中英说不一样的话)
- ✅ **检验力"还要等多久"**(bf857cd)：「无定论」原本把"等 4 年"和"等 226 年"说成同一句话;
  同批修了个股规律表只报全样本 n(分半门实际面对的是半样本,AAPL 月份 26 vs **13**)

**09-16 完成（核实过，别再当待办）：**
- ✅ **取数防历史塌缩**(3f6285e)：`^VIX3M` 的 `yf.download` 返回空 → 落到
  `Ticker.history("3mo")` 兜底，而**该兜底立刻写缓存**，把 6717 行覆盖成 1 行；
  `_record_health` 只查 `rows==0` 和日期新鲜度 → 写出 `{"rows":1,"status":"ok"}`、
  前端显示绿字「新鲜 ✓」。`data/raw` 在 gitignore 里 → **必须写入前拦**。
  修法：`_save_series` 比对已有缓存，新序列 <90% 且缓存 ≥100 行 → 保留缓存不覆盖 +
  status `shrunk` + freshness 打成非 ok。三处缓存写入全部改走此闸。10 条守门、三变异验过。
  ⚠ **本机 VIX3M 历史已丢**（守卫是事故之后加的，拿不回来）→ 本地跑 autodiscovery 时
  `vix_backwardation` 会因样本不足掉出因子族。**别把本地产物提交/发布**，以 CI 产物为准。
- ✅ **`_sign(nan)` 静默返回 -1**(1361f3c)：`abs(nan)<1e-12` 与 `nan>0` 都为 False → 落 -1，
  等于给空/退化观测窗**编出一个"看跌"方向号**，还会随 `full_sign` 进公开 JSON、
  被 `_classify` 当既定假说方向判 confirmed/overturned。旧 `test_sign` 只测了 `None→0`。
  这条是**独立审规格**发现的（不是我找到的）；修的是 6 个已接线族共用的 `_sign`。
- ✅ **因子族可观测期基率**(见下方 F3)：暖机 NaN 被算进对照组 → 基率被压低、边际被放大。

---

## 🔬 2026-09-16 三个实测发现（写因子族 OOS 规格时挖出来的，优先级高于原任务）

> 起因：用户拍板"先a(因子族门4接线)再b"。写规格的过程本身挖出三件比接线更重的事，
> 独立审规格又回 REQUEST-SPEC-CHANGES。**原任务(接线)现在是四件里价值最低的**。

- **F1 ⬛ 主线在办：判定边界落在蒙特卡洛噪声之内**（规格 `docs_internal/SPEC_MC_RESOLUTION.md` R2）
  > ⚠ **本条曾记录过错数字，已更正**：R1 的所有数字读的是
  > `data/processed/autodiscovery.json` —— 那是 **41 天前的快照**（n_survive=6）。
  > 线上产物在 `web/`+`docs/`。**以后凡引产物数字，一律读 `docs/`，绝不读 `data/processed/`。**

  · **FDR 的刀口落在「1 次穿越」与「3 次穿越」之间**（线上 09-15，k=**14**）：
    rank 1–5 自助 **0 穿越/2000** · rank 6–13 置换 **0 命中/1000** ·
    rank 14 自助 **1 穿越/2000**（`golden_cross_sp500`，公开存活者，也正是那条只等对照组的
    KB 晋升候选）· rank 15 自助 **3 穿越/2000** → 被判 **dead**。
    `p=0.0000` 不是"极显著"，是"低于估计器能报的最小值"——X=0/B=2000 的双侧 95% 上界 ≈0.003，
    与那条 0.0030 的 dead **统计上分不开**。
  · **只换随机种子（数据与方法全不动）→ 平均 4.55 条裁决变**、这份 7 条名单原样复现概率 **6.7%**。
    逐条"还在名单上"的概率：legacy_noncomm 88.0% · BTC_mom20_pos 87.5% · p5_h1 83.0% ·
    september 67.9% · monthof_9 67.8% · golden_cross 61.2% · **世界杯年 35.9%**。
    反向：`BTC_mom20_neg`（现 dead）有 10% 概率是 survive。
  · **修法不是全局提高 B**：实测（机器空闲、跑两遍一致）`compute_results` **119s**、
    自助 521 次 **67.8s**（占 57%）→ 全局 B=20000 约 **12.2 分钟**。
    而真实预算 ≈**12 分钟**（CI job 30 分钟 **且 pipeline 失败会整条重跑一遍**），
    `compute_results` 只是 run_all 的一步 → 撑爆。走**边界带自适应加算**（约 +67s）。
  · **判据不能用「离阈值几个标准误」**（独立审规格 B2，已复核）：BY 是 step-up，
    低 rank 候选的 `p_i vs c_i` 不是它的约束；下限附近 SE≈p 使该判据**退化成恒真**；
    `c_i` 在并列组内由**枚举顺序**决定。而且它会把 rank=1（最稳）永久标成"分辨不了"
    = 把证据说得比实际**弱**。→ 改成**重抽 MC 计数、重跑 adjudicate、报改判概率**
    （纯算术，K=2000 约 1.2s，能进 CI 守门）。
  · ⚠ **并列 bug 我判错过并已收回**：曾怀疑 `bh_reject` 按 rank 下标拒绝会导致"同 p 不同裁决"。
    回放账本 62 个交易日 × 13 个池：咬到 **0 次**。原因是**数学上不可能**——BY 临界值 `c_i` 递增，
    并列组（同值 v，ranks a..b）若某 i 满足 `v ≤ c_i`，则组内所有 j>i 也满足 → k 必 ≥ b。
    `set(order[:k])` 与按阈值拒绝**等价**。**那段代码是对的，别照我的错判去"修"。**
  · 已落地：**C0** 金标准(`c1a698e`) · **C1a** 原始计数 `(X,n)` 进产物(`793f9c0`) ·
    **C4a** 改判概率 + 前端展示。**待做**：C2(自助 p 加 +1 平滑，修既有口径不一致) +
    C3(加算) **必须同批发布** —— 实测只平滑会让 `golden_cross` 掉出名单、
    `flicker.py` 会把这次代码变更记成"规律自己在横跳"，还会把它踢出 KB 晋升队列。

- **F2 ⬛ 待办：状态型 sel 未做 block 放大，违反项目自己的 H-3**
  · `autodiscovery` 明文立了规矩：「状态型 sel（可连续多周）→ block = hold + EXTRA，**绝不 block=hold**」，
    `POSITIONING_BLOCK_EXTRA=51`、`TRAILING_BLOCK_EXTRA=77`，两族都照办。
    而**因子族是全系统最状态型的**：sel 连续段长 p90 最大 **144**、max **582**
    （`BTC_above_ma200` p90=143、`NASDAQ_above_ma200` p90=136），用的却是 `block=HORIZON`=20。
  · 实测放大后：`BTC_above_ma200` p 0.118→0.309、recent_p **.083→.265**（.083 贴着 q=0.10 存活线
    → under-blocking 让它**看起来比实际更接近存活**，下次刷新有出现一条本不该存活的公开规律的活风险）。
  · **好消息**：唯一的公开存活者 `BTC_mom20_pos` 段长 p90 恰好=20 → 不受影响，**不需要撤回已发布结论**。
  · 必须**独立提交 + 独立审**：改 15 个 p 会扰动跨族 BY-FDR 阈值、可能影响**其他族**的 verdict。
    且必须在 F1 之后做，否则伪翻转与真变化分不开。

- **F3 ✅ 已修：暖机 NaN 被当对照日，基率被压低、因子边际被系统性夸大**（本节下方"09-16 完成"）

- **F4 ⏸ 原任务(因子族门4接线)：四件里价值最低，压到最后**
  · 接完 **15 只仍全 pending**：锚点全是 2026-06-26、`MIN_OOS_N=30` 两组都要、
    前向窗 20 日吃掉尾部 → 锚后可观测仅 35 行 < 60，**物理上不可能**够判。
    用户可见变化只有 note 措辞从「代码没接」变成「样本还在攒」。
  · 规格草稿在 `docs_internal/SPEC_FACTOR_OOS.md`（**未提交**，需按审查的 5 条 BLOCKER 大改）。
    审查纠正了我三个数字：`NASDAQ_above_ma200` 被我错归进"无影响"档（实为 −0.39pp）、
    `build_feature_df` 是 **0.60s** 不是 2.25s（我把 import 算进去了）、
    `full_sign` 与 `ASSUMED_DIR` 不符的是 **4 只**（dxy_falling / rsi_overbought /
    rsi_oversold / vix_backwardation）不是 1–2 只。
  · 另两条审查 BLOCKER：`survivors_live` 在 run_all 里**排在 `knowledge_base` 之前**
    → "读 oos 产物问门本身"会读到上一轮数据；§2.5 的"约可判日期"用 i.i.d. 外推，
    对聚簇状态族系统性乐观 1.3–2.0 倍（p90 达 2–4 倍）。

---

## 📋 下一步计划（2026-09-07 收工时定·基于当轮全部核实）

### ⏰ 先看一眼,不用动手
1. **今晚 13:00 UTC 的定时刷新** = CI 修复(fe8af0a)的**第一次自然验证**。
   此前只有手动 dispatch + 一次人造 bot-HEAD 跑。**绿了才算真过关**
   —— 教训就是"手动跑绿 ≠ 验证过,还得问 HEAD 是什么形状"。
2. **LLM 英文版的 429 频率**：观察几天。本地实测已撞到限流(fail-soft 正常,下轮自愈)。
   若英文经常缺 → 免费额度不够,方案 B = 只翻日读、周/月读标"中文原文"。

### 🙋 需要用户拍板（产品取舍,不是技术判断,别替他定）
- **C. 横跳规律要不要降级展示** —— **2026-09-16 起有量化依据了，但产品取舍仍待拍板**。
  原问题：`世界杯年` 一周翻转 18~19 次、当前裁决只稳 1 天，却和稳了 53 天的 BTC 动量
  并列在"存活规律观察台"。
  **新增的实测口径**（F1 的 C4a，`mc_survive_prob`）：只换统计用的随机种子（数据与方法全不动），
  每条"还留在存活名单上"的概率 ——
    legacy_noncomm 88.0% · BTC_mom20_pos 87.5% · p5_h1_nasdaq 83.0% ·
    september 67.9% · monthof_9 67.8% · golden_cross 61.2% · **世界杯年 35.9%**
  整份 7 条名单原样复现的概率只有 **6.7%**；平均每次重抽 4.55 条裁决会变。
  **世界杯年是唯一低于 50% 的** —— 即它**更可能不在**这张名单上。
  已做（不需拍板的部分）：观察台逐条显示这个概率（绿≥80/灰60-80/琥珀<60），
  <50% 的额外挂一句人话警告（阈值取 50% 而非 70%，否则 7 条里 4 条挂警告 = 警告滥发）。
  **仍待你定**：要不要在**版面**上也降级 —— 单独分组 / 移出主列表 / 维持现状（已有概率+警告）。
- **Valpha150 池子要不要人工过一遍**：已实测漏了 APP / SMCI / VRT / CRWV / DASH。
  刻意不自动加(池子带手写中文名/板块,自动加会开天窗并改变已发布口径)。

### 🔨 可以直接做（按价值排）
- **D. 统计口径两条**(§3 旧记录,优先级低——当前偏保守、错的方向是"更严"):
  ① "主导组一致"用 argmax,而 SSB 显著可能由**低**离群组驱动 → 可改"最大|偏离|组";
  ② 近期窗硬编码 5 年(`365*5`),月份效应因此现代段恒不可测。
- **两个 docs_internal spec 草稿**(SPEC_EVENT_LENS / SPEC_V15_SELFGROW)一直未跟踪,
  要不要提交由用户定(我多次刻意把它们排除在提交外)。

### ⏳ 时间门,但**"等什么"要说准**(2026-09-14 更正)

- **v1.5 自生长 KB 晋升**:09-14 实测 `golden_cross_sp500` 的 **oos_n 已到 33、过了门槛 30**,
  却仍 pending。查下来**卡的不是时间,是缺对照组**:锚点(06-26)之后标普金叉**一天都没断过**
  (`n_ctrl=0`),没有"不成立"的日子可比 → 门算不出 p 值。
  **区别很要紧**:触发组不足=攒时间就行、一定会到;**对照组不足=光等不一定来**,
  得等金叉真的跌破。此前 CLAUDE.md 只写"只能等",把这两种处境混成一句了。
- 同批修了门的诊断:两个条件原本共用一句「锚后**触发组**样本不足」——线上 oos_n=33 却挂这句,
  **是说错话**。现在按组指名道姓并报数("触发 33 / 对照 0(各需 ≥30)"),`n_ctrl` 也进产物,
  KB 队列每条都能解释为什么还没晋升。6 条守门测试(变异验证过)。
- 另:门对**日历族**的 51 条是「锚后样本不足(<1000日)」、**因子族 15 条**是「OOS 待接(§10)」
  —— 后者是**真待办**(代码没接),不是时间门,别混为一谈。

## 运行环境（Windows）

- 用 `py`（Python 3.12），**不要用 `python`**（指向 Microsoft Store 占位程序）
- 跑任何脚本前设 `$env:PYTHONUTF8='1'`（GBK 控制台会让 Unicode 输出崩溃）
- 全量流水线：`py market-analysis/scripts/run_all.py`（约3分钟）；盘中轻量：加 `--light`
- 测试：`py -m pytest market-analysis/tests -q`（必须全绿才能提交流水线改动）
- 前端改动后跑 `py tools/audit_frontend.py` 交叉检查 ID/onclick;交互实测 `py tools/interaction_audit.py [--mobile]`(**桌面和移动端都要跑**)
- 怀疑账本丢过数据时跑 `py tools/ledger_gap_audit.py` —— 以 **git 历史**为参照,
  查「曾经出现过的日期现在没了」。现有防线都抓不住这类:门比的是工作树 vs origin
  (坏版本推上去后两边都缺)、哈希链的 n_rows 在「删 N 加 N」时净额不变。
  实证:autodiscovery_log 曾被 bot 提交删掉 2026-07-06(104行)与 07-11(148行),2 个月没人发现。
- 改 CSP / 引新资源后跑 `py tools/csp_audit.py [--mobile]` —— 真浏览器逐页收
  `securitypolicyviolation`,专抓「自己的资源被自己的策略挡掉」(静态读 meta 看不出来)。
  **已知边界(非该工具能改善)**:全站带 `script-src 'unsafe-inline'` → 挡不住 XSS;meta 形式拿不到 `frame-ancestors`。

## 模型分层委派（用户授权，自主判断不必先问）

分层委派有效，但**编排与判断固定由 Fable 5（主脑）担任**——别被 `switchModelsOnFlag` 的安全误报
切到 Opus 4.8 当默认（聊土壤/生物/光谱等话题易误判）；被切走就 /model 切回 Fable 5。

委派要"强编排"（2026-06-12 用户要求，P2-3 教训）：主脑的价值在**更详细的规格 + 更重的审查**，不是亲力亲为：
- **写规格**：交代给子代理时预判判断点，写成"若遇到 X（如校准曲线倒挂/非单调），停下报告，不要自行决定"
- **审查**：子代理自报完成 ≠ 验收；实质改动跑 Opus 审查（agent-skills:code-reviewer）+ 主脑亲验关键输出
- **分工**：Fable 5 主脑=方法论/统计判断/含判断的改动/写规格/终审；Sonnet=规格明确无判断的机械活；
  Haiku=重命名/复制级琐事；Opus=审查

### 委派工作流（2026-06-12 用户确认，验证有效）

两种排法按任务类型分流：
- **判断密集型**（统计建模、含诚实性判断）：**Fable 建 + Opus 审**（Opus 冷启动新视角，每轮都抓出主脑盲区）
- **规格明确、实现量大**（聚合/展示/工程，如 benchmark 记分卡）：**Fable 详细计划 → Opus 建 → Fable 审**（卸主脑的载）

六步协议（关键是新增"审计划"）：
```
① 主脑写计划 → ② 独立审计划(挑 spec 洞，比建完再改便宜得多) → ③ 建造
→ ④ 独立审实现 → ⑤ 修复 → ⑥ 验证提交
```
- **审实现按风险分级**：公开统计结论/模型链=双审(Fable+全新Opus)；常规聚合/展示=单审即可，别无脑双审
- **铁律：审查者 ≠ 建造者**（不同模型/全新上下文，保证独立）
- 实证：benchmark 那次 Opus 代码很干净，但 Fable 审出了 Opus 自测漏掉的 ordering bug；而那个 bug 本可在"审计划"阶段更早抓到——所以两道审都有价值

### 与用户的协作风格（用户明确偏好）
用户提建议，**主脑要"考虑 + 平衡最优解"，不是顺着点头**。该踩刹车就踩（如建议无脑双审时分级、建议停委派时厘清是误会）。
诚实的技术异议比虚假认同有价值——这是用户要的。

## 执行协议

- **主脑不是 Fable 5 的会话 → 先读根目录 `HANDOVER.md`**（角色分配/任务队列/CI 红手册/架构路线，2026-07-07 起）
- 按 ROADMAP.md 任务编号干活，**每个编号 = 一次提交**，commit message 带任务号
- **先查后动（用户铁律 2026-06-18）**：收到"做 X"先读 `DAILY_LOG.md` + `OPTIMIZATION_LOG.md §4c(试过但否决)` + 🪦坟场 + 📋登记簿 → 报"之前做过没 / 做成啥样 / 有无结果" → 再动手。**每个尝试都登结果（成/败/测量结论），别只记计划**——失败的尝试也是结果（进坟场或 §4c），这样以后"做 X"一读就清楚
- 改流水线 → 提交前全量跑通 + pytest 全绿；改模型 → bump `MODEL_VERSION` 并重跑 walk_forward，
  新旧指标对比写进 commit message
- web/ 改动要镜像到 docs/（run_all 末步会自动做；手改记得 Copy-Item）
- 提交后 push 到 origin main（GitHub Pages 自动部署；CI 每小时也会自动提交数据，push 前先 fetch）

## 方向与边界（2026-06-22 用户重定调：红线是早期护栏，现逐步突破升级）

**早期"测风险不测方向 / 不预测 / 不荐股 / 不深度学习 / 不引框架"是起步纪律，不是永久红线。**
项目正一点点纳入**预测、荐股、深度学习、LLM 生成**——默认诚实统计打底，前沿逐步扩张。
要守的不是"不预测"，而是**敢预测就敢认账**：每个方向判断都留可追责的公开战绩
（tipjar / 观点 / composite 已是范式）。差异化命门 = **诚实计分**，不是又一个吹票机。
→ 别再拿"不预测/不荐股/不深度学习"拦用户的升级请求（这是我反复犯的错）。

逐步纳入（带验证，不是无脑上；遇判断点照"强编排"停下报告）：
- **预测/荐股**：继续扩，渐成主线；每条带免责 + append-only 计分
- **深度学习**：可试；但小样本易过拟合 → 上线前先 walk-forward/CPCV 证它真比简单模型强（诚实工程异议≠拒绝）
- **LLM 生成**：可做（如"大白话日读"）；**必须喂真算出的数据、防瞎编**；API 密钥/成本/澳洲可达先定
- **新数据源**（社交情绪/另类数据，含此前列的 TickFlow/AkShare 等）：可纳入；先确认够长历史能回测 + 澳洲可达
- **多市场**（A股/港/日韩）：暂不做，后续再说
- **多渠道推送**：Telegram 已接（notify_telegram.py）

仍守（质量地基，非红线）：
- 不绕过 verify_output / pytest 门禁
- prediction_log.csv / paper_ledger.csv / autodiscovery_log.csv 是 append-only 账本，绝不手改历史行
- 秘钥（API token 等）只走环境变量/Secrets，绝不进公开仓库
- 公开计分 / 标不确定性（预测越多越要守——这是 Valpha Lab 的护城河）
